set -o vi

# Socialtext stuff
STCONFIG=$HOME/src/st/stconfig
if [ -d $STCONFIG ]; then
    eval `$STCONFIG/stbin/stdefaultenv`
    SCREENUSERS=`cat $STCONFIG/.st-haxors`
    PATH=$PATH:$STCONFIG/stbin
    source $STCONFIG/.aliases-common
fi
